<!DOCTYPE html>
<html ng-app="FantasyInsiders" ng-init="current_user = <%= current_user.to_json %>;">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Fantasy Insiders" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag	"application" %>
    <%= csrf_meta_tags %>
    <base href="/"></base>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="main" role="main">
      <div class="container">
        <div class="navbar navbar-inverse navbar-fixed-top">
          <div class="navbar-inner">
            <div class="container-fluid">
              <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <a class="brand" href="#">Fantasy Insiders</a>
              <div class="nav-collapse collapse">
                <p class="navbar-text pull-right">
                	<!--
                	<div ng-show="userIsLoggedIn">
	                	Logged in as <a href="#" class="navbar-link"></a>
	                </div>
	            	-->
                </p>
                <ul class="nav">
                	<!-- temp disable
                  <li class="active"><a href="#">Home</a></li>
                  <li><a href="#about">About</a></li>
                  <li><a href="#contact">Contact</a></li>
 		             -->
                </ul>
              </div><!--/.nav-collapse -->
            </div>
          </div>
        </div>
			<div ui-view> </div>
        <footer></footer>
      </div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
